# ADR 0008: Floor ≠ failure — a recovery controller, and a deterministic (LLM-free) scaffold primitive

Status: accepted (Phase 1 implemented 2026-06-20)

## Owner / placement

The org's autonomous builder (`scripts/controller_goal.py`, `scripts/scaffold_primitive.py`). This is an
engine decision for the AI Org.

## Context

A live dogfood (2026-06-19/20, the Shagiri marketplace lanes) failed two goals in the same shape:

- **packaging** — the splitter produced ONE atomic leaf (`scope = marketplace/packaging/`); at the floor
  it cannot split, so when its dialectic did not converge the whole goal was lost with no recourse.
- **mock-suite** — the splitter front-loaded a `mocks-harness-scaffold` leaf that every mock depended on;
  it failed at the floor, so all dependent leaves were blocked and the goal failed.

Today `at_floor()` routes a non-converged leaf straight to `leaf_failed_floor` → goal failure. That
conflates two different things: **"this node cannot be decomposed further" (a base case of the SPLITTER)**
with **"the task has terminally failed" (a recovery decision)**. A high-centrality scaffold node is a
*dominator* in the dependency DAG — everything routes through it — so it is a single point of failure, and
greenfield scaffolds are "all-or-nothing" (no known-good state, high coupling of initial decisions, late
verification).

A web investigation (ChatGPT, 2026-06-20; sources below) confirmed this is a known class of problem and
named the standard responses. Distilled by the product owner (terum): **Steer is the intervention bus for
failure anti-patterns; the first anti-pattern is Scaffold; and the scaffold intervention does not need the
LLM at all** — instantiate a trusted template deterministically (the `git_ops` move at the project-bootstrap
level; settledness-not-dumbing, ADR-0005 of this repo).

## Decision

1. **Floor is a base case of the splitter, not a terminal state.** After the floor, control passes to a
   RECOVERY policy, not straight to failure.
2. **Terminal states are not binary.** Replace converged/failed with a taxonomy: `SUCCEEDED`,
   `RETRYABLE`, `REPLAN_REQUIRED`, `BLOCKED_HITL`, `COMPENSATION_REQUIRED`, `CANCELED`.
3. **Recovery is classified, and its interventions are expressed as STEER** (the additive steering of
   ADR-0007, used as the recovery intervention bus — human or controller injecting guidance into the
   running goal): transient → bounded backoff retry; local-deterministic (syntax/type/test) → reflection
   with stderr+failed-tests+diff → local patch; contract/semantic → REPLAN to the nearest contract-owning
   ancestor, invalidating ONLY dependent descendants; capability → alternative executor/model; partial
   side-effects → rollback/compensation; ambiguous/risky → checkpoint + HITL (one precise question).
   Independent budgets (retry/reflection/alt-exec/cost/time) + no-progress detection (same error signature,
   same patch, no improvement in passing-test count).
4. **Scaffold (anti-pattern #1) is handled DETERMINISTICALLY — the LLM is removed from the path.** A
   detected scaffold leaf is not generated by a carrier; it is instantiated from a versioned **template
   primitive** and proven by an **acceptance gate** (install → build → smoke) BEFORE any fan-out. The LLM
   is the fallback only for an unsupported stack (multi-candidate + verifier + HITL).

### Phasing

- **Phase 1 (this ADR, implemented):** `scripts/scaffold_primitive.py` — a deterministic, LLM-free
  template registry (`match_template` / `instantiate` / `acceptance` / `scaffold`); `controller_goal`
  routes a `_declares_smallest` scaffold leaf with a matching template to the primitive instead of the
  dialectic, with the acceptance gate, falling back to the LLM path only on no-match / acceptance failure.
- **Phase 2:** scaffold as a transactional walking-skeleton DAG (repo-init → lock → build → test-harness →
  entrypoint → vertical-slice → checkpoint → fan-out), acceptance gate before fan-out.
- **Phase 3:** the full recovery matrix — replan-to-contract-ancestor, independent budgets + no-progress,
  durable checkpoints.

## Consequences

- The scaffold SPOF that sank mock-suite/packaging is removed for supported stacks: deterministic, cheap
  (zero LLM tokens), and verified before downstream work starts.
- Steer (ADR-0007) gains a second role: the recovery intervention bus. The terminal-state taxonomy rides
  the org's owned State (ADR-0007). Both are "free" because the primitives were already right.
- Honest residual: the template registry starts small (a Python-package skeleton); unsupported stacks
  still go through the LLM path, which keeps the old brittleness until Phase 2/3 land.

## Research basis (sources — ChatGPT investigation, 2026-06-20)

Recorded so the rationale is traceable; identifiers are as named by the investigation — verify URLs before
citing externally (claim-evidence discipline).

| Work | Relevance |
|---|---|
| **ADaPT** (As-Needed Decomposition and Planning) | decompose a subtask only when the executor cannot do it; max-depth is a plain base case → recovery is separate from decomposition. |
| **TDAG** | fixed decomposition propagates an early subtask's failure downstream = "Cascading Task Failure"; don't collapse partial success into a binary global fail. |
| **Reflexion** (Shinn et al.) | NL self-reflection on failure stored in episodic memory for the next attempt; good for local code repair, but without checkpoints it re-runs everything. |
| **Tree of Thoughts** (Yao et al.) | evaluate multiple reasoning paths with lookahead/backtracking; for alternative implementations — but does not roll back side effects. |
| **HTN plan repair** | a primitive task is a childless leaf; on failure, localize and repair/replan the nearest ancestor subtree — formal precedent for "return to the nearest repairable ancestor", not "atomic ⇒ global fail". |
| **MetaGPT** | agents start after receiving prerequisite artifacts; unit-test+debug ≤3×; bad prerequisites (PRD/design/API/scaffold) are inherited by all downstream. |
| **RepoZero / RPG (ACE)** | repository-from-scratch is hard (strong agents 30–55% pass); explicit dependency/planning graph + iterative verification beat free-form one-shot. (recent arXiv — generalize the numbers cautiously.) |
| **ToolMaze** (2026-06 preprint) | "implicit semantic failure" (broken output read as success) is the hard case; complex DAGs loop wastefully → need contract/semantic assertions, not just exception catching. |
| **LangGraph** | first-class node-level retry, checkpointer, interrupt/resume; replan is a graph/router. |
| **Temporal** | durable retry, event history, pause/resume, Signal, compensation; semantic replan is an external planner's job. |
| **OpenHands SDK** | event-sourced state, deterministic replay, human inspection/control. |
| **AutoGen / Magentic-One** | orchestrator updates the plan on stalled progress; pre-execution approval; save/load team state. |
| **SWE-agent** | RetryAgent + multi-attempt selection; `.traj` is for audit/repro, not durable resume. |
| **CrewAI** | guardrail-failure retry, task replay, checkpoint (early), human-feedback routing. |
| **GPT-Engineer** | greenfield generation; manual iteration on generated files (repo archived 2026-04-22). |
| **BabyAGI** | historical task-queue generation/prioritisation; experimental, original archived. |
| **Devin** | closed orchestration; public guidance = independent atomic slices, clear test/build, per-slice human review. |

## Addendum (2026-06-20): Phase 2 = scaffold → Queue fan-out (staging), NOT floor reflection-retry

Phase 2 is implemented as a **staging** of the dialectic, not a recovery retry:

- A greenfield scaffold leaf **seeds the deterministic skeleton** (acceptance-gated: build/import/smoke;
  **NO Linon** — there is no real logic to adversarially verify yet; the skeleton is the foundation, never
  the deliverable), then **FANS OUT its logic via the Queue**: it re-splits the objective into the
  modules/files the implementation needs and becomes an INTERNAL node. The scaffold gives the seams to
  split along (walking-skeleton → fan-out). The node is done only when its logic children are, so a
  **skeleton-only result is structurally impossible**, and a heavy leaf no longer dies atomically at the
  floor (B / packaging). **Linon applies to the logic children, not the scaffold.**
- Staging does double duty on gate placement: *bypass Linon at the scaffold stage* avoids false-rejecting
  a bare skeleton (the loop's cause), and *apply Linon + the real tests at the logic stage* rejects a
  skeleton-only deliverable.

**Rejected alternative (and why):** a floor reflection-retry that re-runs a Linon-rejected leaf with the
findings injected, using a hash of the **Linon findings** as the no-progress guard. Rejected — LLM output
(findings text) varies run to run, so a content-equality no-progress guard is unreliable (never or wrongly
triggers), and retrying a persistent Linon rejection is exactly the infinite-loop risk. The staging
**eliminates the loop's cause** (Linon never sees the scaffold) instead of bounding it with a fragile
guard. Loop safety for the fan-out is **deterministic only**: depth < `FLOOR_MAX_DEPTH` + the global
budget (counters, never LLM-content comparison).
