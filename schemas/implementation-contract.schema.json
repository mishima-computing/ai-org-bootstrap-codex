{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ai-quality-bootstrap.implementation-contract.v1",
  "type": "object",
  "required": [
    "role_id",
    "contract_id",
    "objective",
    "selected_direction",
    "rejected_parts",
    "implementation_summary",
    "acceptance_criteria",
    "files_allowed_to_change",
    "files_not_allowed_to_change",
    "required_checks",
    "security_requirements",
    "nonfunctional_requirements",
    "non_goals",
    "risks",
    "fallback_plan",
    "handoff_to_implementer"
  ],
  "properties": {
    "role_id": {
      "type": "string",
      "const": "aufheben-designer"
    },
    "contract_id": {
      "type": "string"
    },
    "objective": {
      "type": "string"
    },
    "selected_direction": {
      "type": "string"
    },
    "situation_read": {
      "type": "string",
      "maxLength": 1100
    },
    "rejected_parts": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "source",
          "rejected_part",
          "reason"
        ],
        "properties": {
          "source": {
            "type": "string",
            "enum": [
              "aggressive-designer",
              "conservative-designer",
              "genius"
            ]
          },
          "rejected_part": {
            "type": "string"
          },
          "reason": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    "implementation_summary": {
      "type": "string"
    },
    "acceptance_criteria": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "files_allowed_to_change": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "files_not_allowed_to_change": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "required_checks": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "security_requirements": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "nonfunctional_requirements": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "non_goals": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "risks": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "fallback_plan": {
      "type": "string"
    },
    "handoff_to_implementer": {
      "type": "string"
    },
    "profile_applications": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "profile_id",
          "source_proposal",
          "contract_obligations",
          "acceptance_criteria_refs",
          "required_evidence"
        ],
        "properties": {
          "profile_id": {
            "type": "string"
          },
          "source_proposal": {
            "type": "string"
          },
          "contract_obligations": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "acceptance_criteria_refs": {
            "type": "array",
            "items": {
              "type": "integer"
            }
          },
          "required_evidence": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "additionalProperties": false
      }
    },
    "deliverable_kind": {
      "description": "ADR-0009: the kind of artifact this contract delivers. Selects which conformance profile is required and black-box checked against the built artifact. Declare it explicitly on every contract — use 'none' when there is no executable interface to pin, so the interface obligation is never skipped by simply omitting the field. Schema-optional today (shadow-first via the pre-flight gate); becomes schema-required once profiles cover all kinds.",
      "type": "string",
      "enum": [
        "cli",
        "library",
        "http_service",
        "rpc_service",
        "batch_job",
        "none"
      ]
    },
    "conformance": {
      "description": "ADR-0009: the EXECUTABLE part of the contract \u2014 what the deterministic gate runs against the built artifact. A discriminated profile per deliverable_kind; only the kind's own profile is required (oneOf-style, so the schema never degrades into a universal checklist).",
      "type": "object",
      "properties": {
        "cli": {
          "$ref": "#/$defs/cli_profile"
        },
        "library": {
          "$ref": "#/$defs/library_profile"
        },
        "http_service": {
          "$ref": "#/$defs/http_service_profile"
        },
        "rpc_service": {
          "$ref": "#/$defs/rpc_service_profile"
        },
        "batch_job": {
          "$ref": "#/$defs/batch_job_profile"
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false,
  "allOf": [
    {
      "$comment": "When this contract declares a CLI deliverable, it MUST carry a checkable cli conformance profile \u2014 the design agent chooses and encodes the interface; the gate verifies it (ADR-0009).",
      "if": {
        "required": [
          "deliverable_kind"
        ],
        "properties": {
          "deliverable_kind": {
            "const": "cli"
          }
        }
      },
      "then": {
        "required": [
          "conformance"
        ],
        "properties": {
          "conformance": {
            "required": [
              "cli"
            ]
          }
        }
      }
    },
    {
      "$comment": "A http_service deliverable MUST carry a checkable http_service conformance profile (ADR-0009).",
      "if": {
        "required": [
          "deliverable_kind"
        ],
        "properties": {
          "deliverable_kind": {
            "const": "http_service"
          }
        }
      },
      "then": {
        "required": [
          "conformance"
        ],
        "properties": {
          "conformance": {
            "required": [
              "http_service"
            ]
          }
        }
      }
    },
    {
      "$comment": "A library deliverable MUST carry a checkable library conformance profile (ADR-0009).",
      "if": {
        "required": [
          "deliverable_kind"
        ],
        "properties": {
          "deliverable_kind": {
            "const": "library"
          }
        }
      },
      "then": {
        "required": [
          "conformance"
        ],
        "properties": {
          "conformance": {
            "required": [
              "library"
            ]
          }
        }
      }
    }
  ],
  "$defs": {
    "cli_profile": {
      "description": "Black-box CLI contract: declared install, entrypoint, exit-code policy, and concrete invocation->outcome examples. The checker installs via the commands, runs each example, and compares exit status + stdout/stderr. Exit code is pinned because it is the largest observed interface leak.",
      "type": "object",
      "required": [
        "entrypoint",
        "examples"
      ],
      "properties": {
        "build_and_install": {
          "type": "object",
          "properties": {
            "commands": {
              "description": "Shell commands that build/install the artifact in a clean environment, run in order before the examples. A non-zero exit is a critical conformance finding (the artifact cannot be built).",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "produced_artifacts": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "additionalProperties": false
        },
        "entrypoint": {
          "type": "object",
          "required": [
            "invocation"
          ],
          "properties": {
            "invocation": {
              "description": "How the CLI is launched (e.g. `mytool` or `python -m mypkg`). Examples' invocations are interpreted relative to this entrypoint.",
              "type": "string"
            },
            "working_directory": {
              "type": "string"
            },
            "environment": {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            }
          },
          "additionalProperties": false
        },
        "status_and_errors": {
          "description": "The exit-code policy the examples are expected to honour. Recorded so the checker (and a reviewer) can see the intended categories, not just per-example codes.",
          "type": "object",
          "properties": {
            "success_codes": {
              "type": "array",
              "items": {
                "type": "integer"
              }
            },
            "invalid_input_codes": {
              "type": "array",
              "items": {
                "type": "integer"
              }
            },
            "operational_failure_codes": {
              "type": "array",
              "items": {
                "type": "integer"
              }
            }
          },
          "additionalProperties": false
        },
        "examples": {
          "description": "Concrete golden invocations. Each pins the exit status; stdout/stderr are checked by substring (preferred \u2014 robust to volatile output) or, when given, by normalized exact match. Avoid indiscriminate whole-output snapshots.",
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": [
              "invocation",
              "expected_status"
            ],
            "properties": {
              "invocation": {
                "description": "Arguments appended to the entrypoint (e.g. `--help`, `build bad-input`). The empty string is the no-argument invocation.",
                "type": "string"
              },
              "stdin": {
                "type": "string"
              },
              "expected_status": {
                "type": "integer"
              },
              "expected_stdout": {
                "description": "Exact stdout after normalization (trailing whitespace, CRLF). Use sparingly; prefer expected_stdout_contains.",
                "type": "string"
              },
              "expected_stdout_contains": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "expected_stderr_contains": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },
    "library_profile": {
      "description": "Black-box library contract: import the built module and assert its declared public surface resolves. The checker runs an import probe (import the module, then check each exported symbol is present) THROUGH the same runner as the CLI checker \u2014 it never imports the built artifact in-process. Signatures and an API-diff against a baseline are a later addition; this first cut pins module-importability + exported-symbol presence, the load-bearing 'the public surface exists' contract.",
      "type": "object",
      "required": [
        "module",
        "exported_symbols"
      ],
      "properties": {
        "build_and_install": {
          "type": "object",
          "properties": {
            "commands": {
              "description": "Shell commands that build/install the package in a clean environment, run in order before the import probe. A non-zero exit is a critical conformance finding.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "additionalProperties": false
        },
        "module": {
          "description": "The importable module path the probe imports, e.g. `jsonpick` or `mypkg.core`.",
          "type": "string",
          "minLength": 1
        },
        "import_paths": {
          "description": "Directories prepended to sys.path before the import, relative to the workspace (default: the workspace root). Use when the package is importable from a source dir without being installed.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "exported_symbols": {
          "description": "Attribute names that MUST resolve on the imported module (functions, classes, constants) \u2014 the declared public surface. Each missing one is a major finding.",
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string"
          }
        },
        "python": {
          "description": "Interpreter used to run the import probe (default `python3`).",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "http_service_profile": {
      "description": "Black-box HTTP service contract: declared start command, readiness URL, and concrete request->response examples. The checker starts the service, waits until base_url returns any HTTP response, then compares status and capped body substrings for each example.",
      "type": "object",
      "required": [
        "start",
        "base_url",
        "readiness_timeout_seconds",
        "examples"
      ],
      "properties": {
        "start": {
          "type": "object",
          "required": [
            "command"
          ],
          "properties": {
            "command": {
              "description": "Shell command used to start the service in the conformance sandbox.",
              "type": "string",
              "minLength": 1
            }
          },
          "additionalProperties": false
        },
        "base_url": {
          "description": "Base URL polled for readiness and used to resolve example paths.",
          "type": "string",
          "format": "uri"
        },
        "readiness_timeout_seconds": {
          "description": "Maximum time to wait for base_url to produce any HTTP response before skipping derived example checks.",
          "type": "number",
          "minimum": 0
        },
        "examples": {
          "description": "Concrete golden HTTP requests. Each pins the response status; body checks are substring matches against a capped decoded response body.",
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": [
              "method",
              "path",
              "expected_status"
            ],
            "properties": {
              "method": {
                "type": "string",
                "enum": [
                  "GET",
                  "POST"
                ]
              },
              "path": {
                "type": "string"
              },
              "json": {
                "description": "Optional JSON request body sent with the example.",
                "type": [
                  "object",
                  "array",
                  "string",
                  "number",
                  "boolean",
                  "null"
                ]
              },
              "expected_status": {
                "type": "integer"
              },
              "expected_body_contains": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },
    "rpc_service_profile": {
      "$comment": "ADR-0009 #1 remainder \u2014 EMPTY SLOT, not yet enforced. Intended: the service definition (Protobuf/gRPC or AsyncAPI) plus example call/response pairs. Checker boots the service and drives it over the RPC/message transport, with Buf-style backwards-compatibility checks against the baseline. Filled when an rpc_service deliverable first needs it.",
      "type": "object"
    },
    "batch_job_profile": {
      "$comment": "ADR-0009 #1 remainder \u2014 EMPTY SLOT, not yet enforced. The closest sibling to cli_profile: run the job and assert exit status plus produced_artifacts (files/rows/objects) against declared expectations. Likely reuses most of the cli checker; kept a distinct slot so its run-then-inspect-outputs semantics are explicit. Filled when a batch_job deliverable first needs it.",
      "type": "object"
    }
  }
}