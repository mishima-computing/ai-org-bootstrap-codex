[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-org-bootstrap-codex-workspace"
version = "0.1.0"
description = "Workspace for the Codex-native AI Org Bootstrap kit."
requires-python = ">=3.11"

[tool.setuptools]
packages = []
